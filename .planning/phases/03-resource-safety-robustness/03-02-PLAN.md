---
phase: 03-resource-safety-robustness
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/api/books/[id]/file/route.ts
  - src/app/api/scan/failures/route.ts
  - src/components/FailedImportsBanner.tsx
  - src/app/page.tsx
  - tests/scanner.test.ts
  - tests/fixtures/valid.epub
  - tests/fixtures/valid.pdf
  - tests/fixtures/corrupt.epub
autonomous: true
requirements: [ROBUST-03, ROBUST-05, TEST-03]
must_haves:
  truths:
    - "A book download streams from disk instead of buffering the whole file into memory"
    - "A Range request returns 206 Partial Content with a correct Content-Range for the requested slice"
    - "An unsatisfiable range returns 416; a missing/invalid range returns the full file with Accept-Ranges: bytes"
    - "A signed-in user sees a dismissible banner listing failed imports by file basename and reason"
    - "The failures endpoint is session-gated and never returns a full filesystem path"
    - "The three scanFile branches and malformed-archive handling are covered by tests against a real ephemeral SQLite DB"
  artifacts:
    - path: "src/app/api/books/[id]/file/route.ts"
      provides: "Streaming file download with Range support"
      contains: "createReadStream"
    - path: "src/app/api/scan/failures/route.ts"
      provides: "Session-gated GET returning failed imports (basename + reason)"
      exports: ["GET"]
    - path: "src/components/FailedImportsBanner.tsx"
      provides: "Dismissible failed-import banner in the library UI"
    - path: "tests/scanner.test.ts"
      provides: "scanFile branch tests + malformed-archive → FailedImport test"
      contains: "scanFile"
  key_links:
    - from: "src/app/api/books/[id]/file/route.ts"
      to: "fs.createReadStream"
      via: "ReadableStream / Readable.toWeb response body"
      pattern: "createReadStream"
    - from: "src/app/page.tsx"
      to: "src/components/FailedImportsBanner.tsx"
      via: "rendered in the library page"
      pattern: "FailedImportsBanner"
    - from: "src/components/FailedImportsBanner.tsx"
      to: "/api/scan/failures"
      via: "SWR fetch"
      pattern: "/api/scan/failures"
    - from: "tests/scanner.test.ts"
      to: "scanFile"
      via: "drives scanFile directly against fixtures + ephemeral DB"
      pattern: "scanFile"
---

<objective>
The user-facing half of Phase 3: the book-download route streams with Range
support (no whole-file buffering), failed imports surface as a dismissible
banner backed by a session-gated endpoint, and the scanner's three reconcile
branches plus malformed-archive handling get their first tests.

Purpose: Streaming caps download memory and enables seekable PDF/EPUB fetches;
the failures surface turns silent data loss into a visible signal (basename
only — no path leak); the scanner tests guard the highest-value untested logic
in the repo (a regression here corrupts the library).

Output: a streaming Range-aware file route, GET /api/scan/failures + a banner
component wired into the library page, and tests/scanner.test.ts with committed
fixtures.

Depends on 03-01: the FailedImport model + recordFailedImport/clearFailedImport
helpers must exist before the failures endpoint, the banner, and the
malformed-archive test can assert against them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-resource-safety-robustness/03-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/phases/03-resource-safety-robustness/03-01-PLAN.md

<read_first>
Read these before editing. Each is a file this plan modifies or whose pattern it mirrors.

- src/app/api/books/[id]/file/route.ts — the current whole-file fs.readFile at line 28 returning a Uint8Array body. PRESERVE: the path.resolve(book.filePath) defense (serve only the scanner-recorded path, never user input), the MIME map, the Content-Disposition filename* encoding, and the 404 on missing book / missing file.
- src/lib/scanner/failed-imports.ts (created in Plan 01) — recordFailedImport / clearFailedImport and the FailedImport model the failures endpoint reads.
- src/lib/current-user.ts — getCurrentUser() returns the session user or null; getCurrentUserId() throws UnauthenticatedError; authError(e) maps the typed errors to 401/403. The failures endpoint is session-gated using these.
- src/app/api/scan/status/route.ts — the minimal GET route shape (NextResponse.json, prisma import) to mirror for the failures endpoint.
- src/app/page.tsx — the "use client" library page using useSWR; render the banner near LibraryHeader. Mirror its fetcher.
- src/components/LibraryHeader.tsx + src/components/Section.tsx — the calm educational tone, zinc/amber palette, and lucide-react icon usage to match for the banner.
- tests/isolation.test.ts — the established test seam: vi.hoisted() builds a temp-file PrismaClient, vi.mock("@/lib/prisma") injects it, execFileSync("npx", ["prisma","migrate","deploy"]) applies committed migrations to the temp DB. Mirror this for scanner.test.ts.
- tests/helpers/test-db.ts — makeTestDb() (temp SQLite + migrate deploy + bound client) and seed helpers; reuse for the scanner suite.
- vitest.config.mts — environment "node", globals false (import describe/it/expect/vi explicitly), setupFiles ./tests/setup.ts.
</read_first>

<interfaces>
<!-- Contracts the executor needs. Use directly. -->

From src/lib/current-user.ts:
```typescript
export async function getCurrentUser(): Promise<{ id: string; role: string } | null>;
export async function getCurrentUserId(): Promise<string>; // throws UnauthenticatedError
export function authError(e: unknown): NextResponse; // 401 / 403
```

From src/lib/scanner/failed-imports.ts (Plan 01) and the FailedImport model:
```typescript
// model FailedImport { id, filePath @unique, format, error, createdAt }
export async function recordFailedImport(filePath: string, format: string, error: unknown): Promise<void>;
export async function clearFailedImport(filePath: string): Promise<void>;
```

From src/lib/scanner/index.ts — the functions the test drives directly:
```typescript
export async function scanFile(filePath: string): Promise<void>;
export async function removeFileFromLibrary(filePath: string): Promise<void>;
export function isBookFile(filePath: string): boolean;
```

Range/streaming shape (Next 15 App Router route handler, node:fs + node:stream):
```typescript
import { createReadStream } from "node:fs";
import { Readable } from "node:stream";
// const size = (await fs.stat(filePath)).size;
// for a valid "bytes=a-b": status 206, headers Content-Range: `bytes a-b/size`, Content-Length = b-a+1
// stream = createReadStream(filePath, { start, end });
// body = Readable.toWeb(stream) as ReadableStream;  // or wrap manually
// no/invalid range: status 200, full file, Accept-Ranges: bytes
// unsatisfiable (start >= size, or end < start): status 416, Content-Range: `bytes */size`
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Streaming file download with Range support (ROBUST-03)</name>
  <files>src/app/api/books/[id]/file/route.ts</files>
  <action>
    Replace the whole-file fs.readFile (line 28) with a streamed response (per D ROBUST-03). Keep path.resolve(book.filePath) and the existing 404s (missing book, missing file). Use fs.stat for the size. Parse the request's Range header:
    - No Range or an unparseable/multi-range header: return 200 with the full file streamed via fs.createReadStream, plus Accept-Ranges: bytes and Content-Length: size.
    - Valid single "bytes=a-b" (b optional → to end; suffix "bytes=-n" → last n bytes): clamp end to size-1, return 206 with Content-Range: bytes a-b/size, Content-Length for the slice, Accept-Ranges: bytes, and fs.createReadStream(filePath, { start, end }).
    - Unsatisfiable (start >= size, negative start after parsing, or start > end): return 416 with Content-Range: bytes */size.
    Adapt the Node stream to a web ReadableStream for the Response body (Readable.toWeb, or a manual ReadableStream wrapper — discretion). Preserve the MIME map, the Content-Disposition filename* encoding, and the private Cache-Control. Reject malformed/negative/oversized ranges defensively (do not trust the header arithmetic).
  </action>
  <verify>
    <automated>grep -q "createReadStream" "src/app/api/books/[id]/file/route.ts" && grep -q "Accept-Ranges" "src/app/api/books/[id]/file/route.ts" && grep -q "Content-Range" "src/app/api/books/[id]/file/route.ts" && grep -Eq "206|416" "src/app/api/books/[id]/file/route.ts" && grep -q "path.resolve" "src/app/api/books/[id]/file/route.ts" && ! grep -q "fs.readFile" "src/app/api/books/[id]/file/route.ts" && echo PASS</automated>
    <human-check>HOST-RUN: `curl -I` returns Accept-Ranges: bytes; `curl -r 0-1023` returns 206 + Content-Range bytes 0-1023/SIZE and 1024 bytes; an out-of-range request returns 416; memory stays flat downloading a large book.</human-check>
  </verify>
  <done>Route streams via createReadStream; valid Range → 206 + correct Content-Range/Content-Length; no/invalid range → 200 full + Accept-Ranges; unsatisfiable → 416; path-escape defense, MIME, and Content-Disposition preserved; no whole-file fs.readFile remains.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Failed-imports surface — GET /api/scan/failures + dismissible banner (ROBUST-05 surface)</name>
  <files>src/app/api/scan/failures/route.ts, src/components/FailedImportsBanner.tsx, src/app/page.tsx</files>
  <action>
    Create src/app/api/scan/failures/route.ts: a session-gated GET (per D ROBUST-05). Resolve getCurrentUser(); if null return authError(new UnauthenticatedError()) (401) — or reuse getCurrentUserId() in a try/catch with authError. Query FailedImport ordered by createdAt desc. Return hand-shaped JSON: { failures: [{ id, name, reason, format, failedAt }] } where name is path.basename(filePath) ONLY — never the full path (privacy: no home-dir paths to the client) — and reason is the (already-truncated) error message. Do not expose filePath.

    Create src/components/FailedImportsBanner.tsx ("use client"): SWR-fetch /api/scan/failures; render nothing when the list is empty or dismissed. When non-empty, show a calm dismissible banner (zinc/amber tone matching LibraryHeader, a lucide-react icon e.g. AlertTriangle) listing each failure as "basename — reason". Dismiss is local component state (a close button); re-renders on refresh if failures persist. Use the page's fetcher style.

    In src/app/page.tsx, import and render <FailedImportsBanner /> near the top of <main>, below LibraryHeader and above the Continue-reading Section.
  </action>
  <verify>
    <automated>grep -q "getCurrentUser" "src/app/api/scan/failures/route.ts" && grep -q "basename" "src/app/api/scan/failures/route.ts" && ! grep -q "filePath" "src/app/api/scan/failures/route.ts" && grep -q "FailedImportsBanner" src/components/FailedImportsBanner.tsx && grep -q "/api/scan/failures" src/components/FailedImportsBanner.tsx && grep -q "FailedImportsBanner" src/app/page.tsx && echo PASS</automated>
    <human-check>HOST-RUN: with a FailedImport row present, the library page shows the banner with the file basename (not a full path) and a dismiss button; signed-out request to /api/scan/failures returns 401.</human-check>
  </verify>
  <done>GET /api/scan/failures is session-gated, returns basename + reason (never filePath), ordered newest-first. FailedImportsBanner fetches it, renders a dismissible amber/zinc banner when non-empty, and is mounted in the library page below the header.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scanner branch tests + fixtures (TEST-03)</name>
  <files>tests/scanner.test.ts, tests/fixtures/valid.epub, tests/fixtures/valid.pdf, tests/fixtures/corrupt.epub</files>
  <behavior>
    - Branch A (hash-match moved file): scanFile on a fixture creates one Book; copy/move the same bytes to a new path and scanFile again → the existing Book's filePath updates, no duplicate row.
    - Branch B (same-path content changed): scanFile a new file at a path, then overwrite that path with different valid bytes and scanFile → same Book.id retained, metadata re-extracted, no duplicate.
    - Branch C (brand-new file): scanFile a fresh fixture → a new Book row is created with extracted (or fallback) title.
    - Malformed archive: scanFile a deliberately corrupt EPUB → extraction throws; driven through the watcher's record path (or by asserting extractEpub throws AND recordFailedImport upserts), a FailedImport row exists for that path rather than the throw escaping unhandled.
  </behavior>
  <action>
    Create tests/scanner.test.ts mirroring tests/isolation.test.ts's seam: vi.hoisted() builds a temp-file PrismaClient, vi.mock("@/lib/prisma") injects it, apply committed migrations via execFileSync prisma migrate deploy (or reuse makeTestDb from tests/helpers/test-db.ts). Stage fixtures into a temp library dir (mkdtemp) and drive scanFile/removeFileFromLibrary directly with absolute fixture paths — do NOT spin up chokidar. Cover branches A, B, C and the malformed-archive → FailedImport case described in <behavior>. For the malformed case, assert recordFailedImport leaves a FailedImport row (import the helper from Plan 01) and that the corrupt file does NOT create a Book. Commit SMALL fixtures: tests/fixtures/valid.epub (a minimal real EPUB: container.xml + a tiny OPF + one xhtml), tests/fixtures/valid.pdf (a few-byte valid single-page PDF), tests/fixtures/corrupt.epub (bytes that are not a valid zip / missing container.xml so extractEpub throws). Keep fixtures byte-small. globals false → import { describe, it, expect, vi, beforeAll, afterAll } from "vitest".
  </action>
  <verify>
    <automated>test -f tests/scanner.test.ts && grep -q "scanFile" tests/scanner.test.ts && grep -q "FailedImport" tests/scanner.test.ts && test -f tests/fixtures/valid.epub && test -f tests/fixtures/corrupt.epub && grep -Eq "describe|it" tests/scanner.test.ts && echo PASS</automated>
    <human-check>HOST-RUN: `npm test` runs tests/scanner.test.ts green — all three branches plus the malformed-archive → FailedImport assertion pass against the ephemeral DB.</human-check>
  </verify>
  <done>tests/scanner.test.ts exists, well-formed (describe/it, vitest imports, ephemeral DB via the repo seam), driving scanFile directly to cover the hash-match-moved, same-path-content-changed, and new-file branches plus a malformed-archive → FailedImport case; small valid + corrupt fixtures committed under tests/fixtures/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → file route | Range header is untrusted input parsed into byte offsets |
| client → failures endpoint | Caller must hold a session; filesystem paths must not leak to the client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Denial of Service | whole-file download buffering | mitigate | stream via createReadStream; Range-sliced responses cap per-request memory (ROBUST-03) |
| T-03-06 | Tampering | Range header arithmetic | mitigate | reject negative/oversized/malformed ranges; clamp end to size-1; 416 on unsatisfiable; never feed unchecked offsets to createReadStream (ROBUST-03) |
| T-03-07 | Information Disclosure | failures endpoint path leak | mitigate | session-gate the endpoint; return path.basename only, never filePath; no home-dir path reaches the client or committed UI copy (ROBUST-05) |
| T-03-08 | Spoofing / Elevation | file route still serves only scanner-recorded paths | mitigate | preserve path.resolve(book.filePath); never accept a user-supplied path (existing defense kept) |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies; nothing to install, no supply-chain checkpoint required |
</threat_model>

<verification>
- File route: `grep createReadStream` present, `! grep fs.readFile`, Accept-Ranges + Content-Range + 206/416 present, path.resolve preserved.
- Failures endpoint: session-gated, `grep basename` present, `! grep filePath` (no path leak), banner mounted in page.tsx and fetching /api/scan/failures.
- Tests: tests/scanner.test.ts well-formed, drives scanFile, asserts FailedImport; fixtures committed.
- HOST-RUN (cannot run in agent env): curl Range checks (206/416/Accept-Ranges), signed-out 401 on failures, `npm test` green, `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- ROBUST-03: file route streams with correct 206/200/416 Range semantics; path/MIME/disposition defenses preserved.
- ROBUST-05 (surface): session-gated /api/scan/failures returns basename + reason (no path leak); dismissible banner in the library UI.
- TEST-03: scanner branch tests + malformed-archive → FailedImport, with committed fixtures, well-formed against the repo's ephemeral-DB seam.
</success_criteria>

<output>
Create `.planning/phases/03-resource-safety-robustness/03-02-SUMMARY.md` when done.
</output>
